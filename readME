If you're utilizing /export cwl, you must delete the 2nd and 3rd sheet to make it work. The code only reads the first 8 sheets and pulls data from specific columns. Failure to remove will print an except error.
